/* Placeholder – WP3 session 3b replaces this with the esbuild output of lib/snippet (pnpm build:snippet). */
try {
  console.log("[shab] placeholder – config from app embed:", window.__shab);
} catch (e) {
  /* never break the merchant page */
}
